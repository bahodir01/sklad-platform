# Dev использует Vite-прокси /api -> http://localhost:8000 (vite.config.ts).
# Переопределить цель прокси можно здесь при необходимости; кросс-оригинного
# VITE_API_URL намеренно нет (Ф-7): base клиента всегда относительный /api/v1.
