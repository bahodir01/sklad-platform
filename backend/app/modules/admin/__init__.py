"""М7 «Администрирование» — read-only алерты админ-панели (Q3 заказчика).

Модуль не вводит новых таблиц (схема заморожена). Алерты ВЫВОДЯТСЯ из состояния
на лету: расхождения остатков — сверкой stock_balances↔SUM(stock_movements),
черновики пополнения касс — чтением артефактов drafts/income/*.json.
"""
